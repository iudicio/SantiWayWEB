# Отключим гостя
user default on

# Сервисный пользователь приложения
# - включён
# - пароль (меняем на свой)
# - разрешим команды чтения/записи, запретим опасные
# - ограничим доступ к ключам только с префиксом "default:*"
user default on > strongpassword ~default:* +@read +@write -@dangerous